# P0.5c clean-staging physical reproduction, 2026-09-20

Question: can the committed observable consumer be restored away from the
active worktree, resolve the exact locked library without a sibling path pin,
re-emit its adopted bundle, and pass the complete physical/check path without
manual edits?

Yes. The durable evidence is
[`flow_results/20260920-081325-ccecd9ed`](../../flow_results/20260920-081325-ccecd9ed/README.md).
It contains the complete immutable input bundle as well as the standard run
archive and command logs.

- Consumer: clean detached protemu
  `8d3ada1571e07ff21e4c8707dcc3ce917d6037b3`.
- Library: locked `hardcaml_asic`
  `a257424c3ae31fd6ee10cea052cc7577f15d2677`, built from a clean detached
  checkout into an isolated prefix and selected through `OCAMLPATH`.
- Build identity:
  `85729c18ca404a4b45dd8b1e97ea5833ad3677614b2064b222b38e9c17f575fe`;
  manifest SHA-256
  `adc6edfd3ba8b34edd799b0c31f76bb5f477f128447ebc2ab255be7abe7ab922`.
- Run ID: `ccecd9edc8e342b2b28f1a88d5c4e7ac`, full stage completed in
  one 26m50s hardening attempt; postcheck completed in 8m24s.
- Target/tools: TT 6x4, IHP CMOS5L, support tools `da63c992...`, PDK
  `2bbec755...`, LibreLane `3.1.0.dev3`, image `sha256:d109140b...`.
  The recorded waiver accepts validated host Python 3.12.3 and container Python
  3.13.13 against the requested 3.11.
- Adoption checks: repeatable emission, manifest/source hashes, conflict
  rejection, both observable RTL traces, Verilator lint, and generic Yosys
  synthesis pass.

| Measurement | Result |
| --- | --- |
| Mapped cells / area | 94 / 1,319.031 um^2 |
| Final standard-cell area / utilization | 2,228.08 um^2 / 0.247% |
| Worst setup slack | **+15.589 ns**, `nom_slow_1p08V_125C` |
| Worst hold slack | **+0.147 ns**, `nom_fast_1p32V_m40C` |
| Timing coverage | fast/slow/typ pass; no setup/hold, slew, capacitance, fanout, or unconstrained-path violations |
| Inferred latches / synthesis errors / unmapped | 0 / 0 / 0 |
| Antenna / routed DRC / Magic DRC / LVS | pass, zero counts |
| TT precheck | 9 of 9 pass, including KLayout SG13CMOS5L DRC |
| Final-netlist observable simulation | pass, `$finish` at 144000 ps |

LibreLane's in-flow KLayout DRC remains disabled by the pinned declaration; TT
precheck provides the established KLayout coverage. The known ABC
`sg13cmos5l_buf_4/X` lookup diagnostic occurs after mapping for this exact
synthesis script and is not used for timing acceptance; post-route OpenSTA is.

Compared with P0.5b, the flow payload, final netlist, metrics, timing, testbench,
and verdicts agree. Build/run identities changed because this emission records
the committed clean inputs and newer locked library provenance. The independently
executed GDS has a different recorded hash; byte-identical physical output is not
claimed. The complete bundle was restored to a fresh directory, passed
preflight, and reproduced every manifest hash and the run-record linkage.

Conclusion: the adopted observable physical path is reproducible from committed
consumer source and recorded dependencies without an undocumented
`../hardcaml_asic` checkout or edits to generated inputs. This closes
`hardcaml_asic` P5.4, not memory hardening, SRAM support, or P5.5.
